#ifndef Polygon_hpp
#define Polygon_hpp
/*
#include "Complex.hpp"
#include "Grapic.h"
using namespace grapic;

namespace Polygon {

const int MAX_VERTEX = 100;
struct Polygon {
	Complex vertex[MAX_VERTEX];
	int vertexCount;
};
Polygon operator+=(Polygon polygon,Complex vertex);
void draw(const Complex& a,const Complex& b);
void draw(const Polygon& polygon);
Polygon lerp(const Polygon& a,const Polygon& b);
Complex center(const Polygon& polygon);
}*/
#endif
